# התקנת הפרויקט — מטעמי בת מלך 🕯️

מסמך הערות לפריסה: Cloudflare Pages לאתר, ו־Railway אם רוצים בהמשך מסד נתונים קטן לסנכרון בין מכשירים.

---

## 1. מה יש בריפו

| קובץ | מה זה |
|---|---|
| `index.html` | **מערכת הניהול** — קובץ אחד, סטטי, בלי תלויות. זה מה שבעלת העסק פותחת. |
| `order-form.html` | **טופס ההזמנה ללקוחות** — הלקוח ממלא ושולח בוואטסאפ עם קוד `#BM1#` שמתייבא למערכת. |
| `app.html`, `order.html` | אותו תוכן בלי עטיפת `<html>` — גרסאות לפרסום כ־Artifact בקלוד. לא רלוונטיות לפריסה. |
| `tests/` | 9 חבילות בדיקות Playwright (ראו סעיף 5). |
| `README.md` | תיעוד שימוש למשתמשת. |

אין build, אין npm ברמת השורש, אין משתני סביבה. שני קובצי HTML סטטיים — זהו.

**איפה הנתונים:** כל הנתונים (הזמנות, לקוחות, תפריט, הוצאות, הגדרות) נשמרים
ב־`localStorage` של הדפדפן תחת המפתח `batmelech-orders-v1` — **פר מכשיר ופר דפדפן**.
אין שרת ואין סנכרון עד שמוסיפים את שלב 4.

---

## 2. פריסה ל־Cloudflare Pages

1. Cloudflare Dashboard ← **Workers & Pages** ← **Create** ← **Pages** ← **Connect to Git**.
2. בוחרים את הריפו `KBRglobal/batmelech` ואת הענף (או ממזגים קודם ל־main).
3. הגדרות build:
   - **Framework preset:** None
   - **Build command:** *(ריק)*
   - **Build output directory:** `/`
4. Deploy. מקבלים `https://<project>.pages.dev`.

כתובות אחרי הפריסה:
- מערכת הניהול: `https://<project>.pages.dev/` (‏`index.html` מוגש אוטומטית בשורש)
- טופס הלקוחות: `https://<project>.pages.dev/order-form`

אופציונלי — כתובות יפות יותר: קובץ `_redirects` בשורש עם:
```
/order   /order-form.html   200
```

### צ'ק־ליסט אחרי הפריסה (חשוב!)

- [ ] להיכנס למערכת ← ⚙️ הגדרות וגיבוי ← **תשלומים וקישורים** ← להדביק את
      **קישור טופס ההזמנה** (ה־URL של order-form) — כדי שייכנס להודעת "התפריט של שבת".
- [ ] להדביק שם גם את **לינק התשלום** (ביט/PayBox) — מפעיל את כפתור "בקשת תשלום".
- [ ] אם היא עבדה עד עכשיו מקובץ מקומי / מה־Artifact: לייצא גיבוי מהמכשיר הישן
      (⚙️ ← שמירת קובץ גיבוי) ולשחזר אותו בכתובת החדשה. **הנתונים לא עוברים לבד** —
      localStorage שייך לדומיין.
- [ ] להגיד לה לעבוד תמיד מאותו דפדפן באותו מכשיר, ולגבות שבועית
      (המערכת מזכירה לבד אחרי 7 ימים).
- [ ] דומיין מותאם (אופציונלי): Pages ← Custom domains.

### הגבלה ידועה

תפריט **טופס הלקוחות** מוטמע בקובץ `order-form.html` ולא קורא את עריכות התפריט
שנעשות במערכת (הן ב־localStorage שלה). שינוי תפריט/מחיר שצריך להופיע גם ללקוחות =
עריכה ידנית של `order-form.html` (או שלב 4).

---

## 3. מבנה הנתונים (לגיבוי ולהגירה)

קובץ הגיבוי שהמערכת מייצאת הוא JSON אחד:

```json
{
  "orders":       [ { "id", "date", "name", "phone", "place", "address", "time",
                      "pickup", "status", "meals", "aricha", "challot",
                      "salads": { "שם סלט": {"o": 1, "p": 0} },
                      "firsts": {}, "heat", "firstsNote", "mains": {}, "mainsNote",
                      "sides": {}, "desserts": {}, "extras": { "שם": {"q":1,"note":""} },
                      "custom": [], "notes", "total", "deposit", "payMethod", "paid" } ],
  "settings":     { "maxMeals", "out": [], "payLink", "orderFormUrl" },
  "menu":         "תפריט ומחירים אם נערכו (null = ברירת מחדל)",
  "customerMeta": { "מפתח-טלפון": { "vip": true, "notes": "" } },
  "prepDone":     { "YYYY-MM-DD": { "קטגוריה|מנה": true } },
  "expenses":     { "YYYY-MM-DD": 210 },
  "lastBackup":   1730000000000
}
```

זהו החוזה להגירה לשרת: טבלה/עמודת JSONB אחת מספיקה.

---

## 4. Railway — מסד קטן (כשרוצים סנכרון בין מכשירים)

**מתי צריך:** כשעוד אדם (רושם המשלוחים, עוזרת) צריך לראות את אותם נתונים,
או כשהיא רוצה טלפון + מחשב יחד, או כשרוצים שהזמנות מהטופס ייכנסו למערכת לבד.

**ארכיטקטורה מינימלית (יום עבודה אחד):**

1. **Railway** ← New Project ← **PostgreSQL** (התוכנית הקטנה, ~5$/חודש).
2. שירות Node קטן (Express/Hono) באותו פרויקט, שלושה endpoints:
   - `GET  /api/state` — מחזיר את ה־JSON כולו (בדיוק במבנה של סעיף 3)
   - `PUT  /api/state` — שומר את ה־JSON כולו (write-through, גרסה אחת)
   - `POST /api/order` — פתוח לטופס הלקוחות: מוסיף הזמנה בסטטוס "חדשה"
3. סכימה: טבלה אחת `state (id text primary key, data jsonb, updated_at timestamptz)` —
   שורה אחת. לא צריך יותר בשלב הזה.
4. **אימות פשוט:** משתנה סביבה `APP_PASSWORD`; הקליינט שולח
   `Authorization: Bearer <password>` (היא מזינה סיסמה פעם אחת, נשמרת ב־localStorage).
   `POST /api/order` נשאר בלי סיסמה אבל עם rate-limit.
5. **CORS:** לאשר רק את הדומיין של ה־Pages.
6. משתני סביבה ב־Railway: `DATABASE_URL` (אוטומטי), `APP_PASSWORD`, `ALLOWED_ORIGIN`.

**חיבור הקליינט — שינוי קטן ומרוכז:** כל הקריאה/כתיבה עוברת דרך שתי פונקציות
ב־`app.html`: `loadStore()` ו־`saveStore()`. מחליפים אותן ב־fetch ל־`/api/state`
(עם debounce לשמירה + fallback ל־localStorage כשאין רשת) — שאר הקוד לא משתנה.
בטופס הלקוחות מוסיפים שליחת `POST /api/order` לצד הוואטסאפ.

**הגירה:** מייצאים גיבוי מהמערכת ← `PUT /api/state` עם הקובץ. גמרנו.

**חלופה באותו חשבון Cloudflare:** ‏Workers + D1 (SQLite) נותן את אותו דבר בלי
Railway ובחינם ברמת השימוש הזו — אותם שלושה endpoints, אותה סכימה. שווה שיקול
אם רוצים הכול תחת ספק אחד.

---

## 5. בדיקות

בתיקיית `tests/` יש 9 חבילות Playwright שמכסות את כל המסכים (73+ בדיקות):
הזמנות, פינוקים, סיכומי הכנות, חיפוש, בונים, מדבקות, משלוחים ומסלול שליח,
לקוחות, כספים, עורך תפריט, ייבוא הודעות (קוד + טקסט חופשי), נתוני דוגמה.

```bash
cd tests
npm install
CHROMIUM_PATH=/path/to/chrome npm test   # או בלי המשתנה אם Chromium במיקום הדיפולטי
```

לפני כל שינוי עתידי בקוד — להריץ. הבדיקות רצות נגד `index.html` המקומי, בלי רשת.

---

## 6. נתוני דוגמה

במסך ⚙️ הגדרות וגיבוי ← "🎭 נתוני דוגמה" — טוען 4 הזמנות אמיתיות מקובץ הוורד
(קטי, לילך, חיים, ניר) על תאריכי שישי הקרובים, כולל VIP, הערות לקוח, הוצאות
ותקרת עומס — כדי לראות את כל המסכים חיים. "מחיקת נתוני הדוגמה" מסירה אותן בלי
לגעת בנתונים אמיתיים (הן מזוהות לפי `id` שמתחיל ב־`demo`).

---

## 7. לוח שבתות (hebcal)

הקובץ `app.html` מכיל טבלה מוטמעת (`SHABBAT_TABLE`) של פרשת השבוע, חגים
וזמן הדלקת נרות לדובאי — ל־130 השבתות הקרובות (~2.5 שנים), מחושבת עם
[hebcal](https://github.com/hebcal). עובדת בלי אינטרנט.

**רענון (פעם בשנתיים, או כשמשנים עיר):**
```bash
npm install @hebcal/core
node tools/gen-shabbat.mjs        # יוצר shabbat-table.json
# מחליפים את התוכן של const SHABBAT_TABLE=... ב-app.html ובונים מחדש את index.html
```
הקואורדינטות והאזור זמן מוגדרים בראש `tools/gen-shabbat.mjs`.

---

## 8. קודי QR

מוטמעת בקובץ ספריית [qrcode-generator](https://github.com/kazuhikoarase/qrcode-generator)
(MIT, Kazuhiko Arase) — עובדת בלי רשת. ה־QR נוצר מהקישורים שבהגדרות
(«תשלומים וקישורים»): QR לטופס ההזמנה מודפס אוטומטית על בונים ומדבקות
(ועל בון של הזמנה שלא שולמה — QR ללינק התשלום במקום), ובכפתור
«הדפסת פלייר עם QR» יש פלייר מעוצב לפרסום בקהילה. אין מה לתחזק —
הקודים נגזרים תמיד מהקישורים העדכניים בהגדרות.
