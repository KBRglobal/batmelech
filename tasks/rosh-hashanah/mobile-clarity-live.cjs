const {chromium}=require('../../tests/node_modules/playwright-core');const assert=require('node:assert/strict'),fs=require('node:fs');
(async()=>{const b=await chromium.launch();try{const c=await b.newContext({locale:'he-IL',viewport:{width:390,height:844}});const p=await c.newPage();const errors=[];p.on('pageerror',e=>errors.push(e.message));await c.route('**/api/**',r=>r.request().method()==='GET'?r.continue():r.abort());const r=await p.goto('https://www.batmelech.ae/rosh-hashanah');assert.equal(r.status(),200);await p.locator('.festive-package-guide').waitFor();await p.locator('input[value="family"]').check();const guide=p.locator('.festive-package-guide');assert.equal(await guide.locator('li').count(),8);assert.ok((await guide.innerText()).includes('$450'));const script=await p.locator('script[src^="/site/assets/"][type="module"]').getAttribute('src');assert.ok(fs.readFileSync('site/index.html','utf8').includes(script));assert.ok(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));await p.setViewportSize({width:1440,height:900});assert.equal(await guide.isVisible(),false);assert.deepEqual(errors,[]);console.log(JSON.stringify({mobileOverview:true,familyContents:8,familyPrice:450,desktopGuideHidden:true,script,errors}));}finally{await b.close()}})().catch(e=>{console.error(e.message);process.exit(1)});
