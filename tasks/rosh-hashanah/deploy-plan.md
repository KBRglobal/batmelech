# Admin billing integration and production release

Authorization: 2026-09-08 user explicitly requested production deployment after verifying that staff can enter and bill holiday packages in the management system with correct prices. Prior festive customer design and exact contents/prices remain approved.

Observed gap: site intake stores holiday selections, but the management editor defaults missing meal quantities to one regular Shabbat meal and cannot configure festive packages. Pickup was previously represented in notes without its structured flag. Server price explanations also lack festive package lines.

Implementation:
1. Reuse shared festive calculator in the admin domain and server price breakdown. Add typed structured festive packages, explicit zero legacy meals for festive-only site orders, and ordinary mixed-cart items as custom lines; retain delivery/pickup and manual discounts.
2. Add a distinct festive package editor to the existing management screen. Same quotas/choices, per-child billing, live breakdown, save/reopen/edit support. Do not merge festival quotas into existing Shabbat categories.
3. Regenerate canonical kitchen details from current selections on staff save; remove stale autogenerated website summary using a bounded marker while preserving customer notes.
4. Verify server/customer/admin equality across base packages, every excess category, mixed fish units, independent children, quantities, delivery, mixed baskets and staff manual overrides. Run full relevant admin domain tests and local browser save/reopen journey.
5. Verify required release footer links and KBR credit using existing business facts, without claiming legal certification. Reuse previous design and responsive evidence unless affected code changes.
6. Build customer and management apps, inspect changes, commit and push the authorized release. Verify deployed commit/assets, public page, authenticated management bundle and read-only management flow. Never create real customer charges or send messages during QA.

Production data: no overwrite of existing menus/settings/orders. Fixed festive catalog and staff editor ship in code. No seeding of fake orders into production. Existing authenticated API/CLI only; secrets never logged.
